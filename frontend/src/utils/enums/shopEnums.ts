export enum SORT {
  DEFAULT = '1',
  POPULARITY = '2',
  PRICE = '3',
  NAME = '4',
}

export enum PRICE {
  MIN = 0,
  MAX = 1000,
}

export enum RANGE_PRICE {
  MIN = 0,
  MAX = 2000,
}

export enum PAGE {
  FIRST_PAGE = 1,
  LIMITED_PRODUCTS = 10,
}
